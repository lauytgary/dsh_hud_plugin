# screenshot

Place a screenshot of the HUD here (e.g. `screenshot.png`) and it will show up
at the top of the README.

Quick way to capture on macOS: press `Shift+Cmd+4`, drag over the HUD column,
then save the file into this folder.
